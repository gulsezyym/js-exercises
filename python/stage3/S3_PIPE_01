def pipe(*fns):
    def run(x):
        for fn in fns:
            x = fn(x)
        return x
    return run


add1 = lambda x: x + 1
double = lambda x: x * 2

fn = pipe(add1, double)
print(fn(3))  # (3+1)*2 = 8
