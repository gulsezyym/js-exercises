def flatten1(lst):
    res = []
    for item in lst:
        if isinstance(item, list):
            for x in item:
                res.append(x)
        else:
            res.append(item)
    return res


print(flatten1([1, [2, 3], [4], 5]))
