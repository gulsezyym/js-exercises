def run_command(cmd: str):
    match cmd:
        case "start": return "STARTED"
        case "stop": return "STOPPED"
        case "status": return "STATUS_OK"
        case _: return "UNKNOWN_COMMAND"


tests = ["start", "status", "stop", "ping"]
for t in tests:
    print(t, "->", run_command(t))
