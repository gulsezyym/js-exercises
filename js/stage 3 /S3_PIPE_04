const processNumbers = pipe(
  arr => arr.map(x => +x).filter(n => !Number.isNaN(n)),
  arr => arr.map(n => n * 2),
  arr => arr.reduce((a, b) => a + b, 0)
);

console.log(processNumbers(["1", "x", "2", "3"])); // (1+2+3)*2 = 12
