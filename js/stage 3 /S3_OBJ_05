function invert(obj) {
  const res = {};
  for (const k in obj) {
    const v = obj[k];
    if (res[v]) {
      if (Array.isArray(res[v])) res[v].push(k);
      else res[v] = [res[v], k];
    } else {
      res[v] = k;
    }
  }
  return res;
}

console.log(invert({ a: 1, b: 2, c: 1 }));
